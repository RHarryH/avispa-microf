# μF

Small tool for generating invoices for Avispa purposes

**Notes**

There is a bug with ideauidesigner-maven-plugin plugin which unables to compile
classes created in GUI Designer using Maven. Workaround is to compile class
manually using IntelliJ and then create the package, which will reuse compiled class.